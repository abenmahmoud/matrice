services:
  api:
    ports:
      - "${API_HOST_BIND:-127.0.0.1}:${API_HOST_PORT:-8090}:8080"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      AI_MODEL: ${AI_MODEL:-gpt-4o}

  frontend:
    ports:
      - "${FRONTEND_HOST_BIND:-127.0.0.1}:${FRONTEND_HOST_PORT:-8091}:80"
